*.conf
!*.example

# Self-signed cert/key generated by tests/containertests/run_tests.py's
# https_enabled fixture. Real production certs come from the /etc/letsencrypt
# bind mount (docker-compose.yml), never from files in this directory.
*.crt
*.key
