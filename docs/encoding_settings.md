Providing any custom encoding ladder via the environment completely annihilates the default .ini ladder.

To add to the config, you must set the entire ladder via env vars.  setting just one env var will tell the system to do only one encoding.